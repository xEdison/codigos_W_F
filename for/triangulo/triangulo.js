let num = parseInt(prompt("ingrese numero"));
let contador = 1;
let acomuladora = "*"
let x = "*"

for (let contador = 1; contador <= num; contador++) {
    console.log(acomuladora)
    acomuladora += x
}

    for (let num = contador; num <= 1; num--) {
        console.log(x)
        contador++   
        
    }
    console.log(x);